# AgentObs

> A local-first observability and control layer for AI coding agents. Most
> tools in this space only report cost after it is spent, and the few that
> enforce stop at a cumulative dollar figure. AgentObs refuses a tool call on
> token budgets, rolling rate-limit windows, command policy and scoped
> approvals - before the call runs.

Install: `npm install -g @klars/agentobs` then `agentobs init`.
Requires Node.js 22.5 or newer. Open source (MIT), no account, no telemetry.
Source: https://github.com/klars-ai/agentobs

## What problem it solves

Most agent tooling is read-only. Tools like ccusage and
Claude-Code-Usage-Monitor parse the same local transcripts and tell you what
you already spent; they cannot stop anything, because reporting usage does not
require knowing your limit. A small number of tools - cost-guardian, Septim
Cost Guard - do block, but only on cumulative dollars. That leaves the cases
that matter most unserved: a subscription user whose dollar cost is zero, a
rolling rate-limit window, a destructive command, a call that should be held
for approval. AgentObs asks for the limit and enforces all four.

## Core capabilities

- **Budgets that block.** `agentobs budget set --daily 5 --block` refuses tool
  calls once the limit is hit. Periods: daily, weekly, monthly, and the
  rolling 5-hour block that matches Anthropic's own rate-limit window.
  Denominated in USD or in tokens, so Max and Pro subscribers - who have no
  dollar cost - can still cap usage.
- **Forecasting.** `agentobs forecast` projects time-to-limit from the burn
  rate of recent real activity, e.g. "at $4.14/hour you hit the limit in
  44 min". Burn rate is measured over active minutes, not wall-clock, because
  averaging idle time understates a live session several-fold.
- **Command policy.** Glob rules over tool inputs deny destructive commands and
  secret-file reads before execution, e.g. `rm -rf /` or reading `.env`.
- **Interactive approvals.** A `needs_approval` rule holds a call and notifies
  you. Approval is fingerprinted over the tool name plus its exact input, so
  approving `rm -rf ./build` never authorises `rm -rf /`. Approvals expire
  after 60 minutes. This is approve-then-retry, not a live prompt: the first
  attempt is always refused, because a hook has no channel to the user.
- **Secret redaction.** Vendor-specific patterns (AWS, Anthropic, OpenAI,
  GitHub, GitLab, Slack, Stripe, Resend, Google, npm, JWT, PEM) plus
  structural rules for assignments, CLI flags, auth headers and URL
  credentials. Nothing unredacted is ever written to disk.
- **MCP server.** `claude mcp add agentobs -- agentobs mcp` lets the agent
  query its own usage - you can ask Claude "am I close to my limit?" and it
  answers from real local data.
- **Statusline.** A compact status-bar line showing the real 5-hour rate limit
  from Anthropic's own payload, the tightest budget, a countdown, and context
  usage.
- **Multi-agent import.** Reads Claude Code, OpenAI Codex CLI and Gemini CLI
  with field names taken from those projects' own source, plus OpenCode and
  Copilot CLI. Sources are declarative, so an unknown agent can be added with
  a JSON definition and no code.
- **Alerts.** `agentobs notify set <url>` sends a budget breach or an approval
  request to Slack, Discord or a user-supplied endpoint. Opt-in with no
  default destination; payloads carry budget names and numbers, never tool
  inputs, and are redacted before sending.
- **Dashboard.** A local web dashboard with light and dark themes. Tabs for
  overview, day-by-day totals, breakdown by project/model/tool, budgets and
  individual tool calls. Time ranges include live windows of the last 1 to 300
  minutes, for watching a run in progress. Tool names carry a plain-English
  description on hover.
- **Optimisation hints.** Derived from the data being viewed: a tool failing
  often, one tool dominating cost, an identical call repeated, a session over 2M
  input tokens, or a dollar budget that cannot bind on a subscription. Each names
  the number that triggered it; nothing is shown when there is nothing to say.
- **Plan-aware cost labelling.** Cost is computed at API list price. On a
  subscription that is not a bill, so the figure is labelled "API-equivalent
  usage" and names the plan, rather than implying money was spent.

## Honesty commitments

These are design rules, not marketing:

- A source is marked `verified` only when its field names come from the
  agent's own source or a real log file - Claude Code, Codex CLI and Gemini
  CLI qualify. OpenCode and Copilot CLI remain `unverified` and say so.
  Copilot CLI is a deliberate example: a real install was checked and it
  persists no usage data at all, so claiming support would mean silently
  recording nothing.
- An unpriced model renders as `-`, never `$0.00`. Unknown is not zero.
- Cache reads are billed at 0.1x and counted separately from fresh input,
  because summing them inflates totals by orders of magnitude. They also
  dominate most real totals - on one measured session, 99.3% of all tokens were
  cache reads, because every turn re-sends the whole conversation.
- Per-call cost is attributed only when a message issued exactly one tool call,
  which is the case in practice. A message issuing several leaves them uncosted
  rather than dividing the total.
- A tool AgentObs does not recognise gets no description rather than a guessed
  one. MCP servers contribute arbitrary names.

## Privacy

Everything is local: a SQLite database under `~/.agentobs`. No account, no
API key, no telemetry. Secrets are redacted before storage - the rules are
unit-tested and have caught real leaks, but no pattern set catches every secret
format, so the database should be treated as sensitive rather than sanitised.

The single exception is outbound alerts, and it is opt-in: nothing is sent
until the user writes a destination into `~/.agentobs/notify.json`, there is
no default endpoint or vendor, and plain http to a remote host is refused so
a webhook secret cannot travel in clear text.

## Limits worth stating

AgentObs is a safety net, not a guarantee, and these are documented rather than
discovered:

- Guardrails fail open by design. A malformed policy degrades to
  allow-everything and says so; in that state nothing is blocked.
- Budgets are enforced between tool calls. A call already running is not
  interrupted, so a limit can be exceeded by the cost of one operation.
- Enforcement runs through Claude Code's hooks. Where hooks do not fire, no
  enforcement happens at all.
- Costs are estimates computed from a local price table, not an invoice.
- Policy rules match tool inputs textually; a destructive command phrased in a
  way the rules do not anticipate is not caught.

Full text: https://github.com/klars-ai/agentobs/blob/main/DISCLAIMER.md

## Links

- Source and issues: https://github.com/klars-ai/agentobs
- Package: https://www.npmjs.com/package/@klars/agentobs
- Site: https://agents.klars.ai
- Contact: contact@klars.ai
