# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored.

# Your own public IP, /32. Never 0.0.0.0/0 - this opens SSH.
ssh_allowed_cidr = "203.0.113.4/32"

domain_name = "agents.klars.ai"
